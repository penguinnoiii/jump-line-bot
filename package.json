{
  "name": "jump-line-bot",
  "version": "1.0.0",
  "description": "Jump Thailand — LINE OA education guidance chatbot (Messaging API webhook + Gemini)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "richmenu": "node --env-file=.env scripts/setup-richmenu.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/genai": "^2.17.1",
    "@line/bot-sdk": "^9.9.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.6"
  }
}
